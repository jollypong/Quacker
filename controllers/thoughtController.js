const { User, Thought } = require('../models');

const thoughtController = {
  // get All Thoughts,
  // add Thought,

  // get thought by ID
  // update Thought,
  // delete Thought,

  // add Reaction,
  // delete Reaction
};

module.exports = thoughtController;